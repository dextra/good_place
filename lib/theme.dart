import 'package:flutter/material.dart';

class CustomColors {
  static const background = Color(0xff847CFF);

  static const dark_gray = Color(0xff212121);
  static const light_gray = Color(0xffF3F5FA);
  static const white = Colors.white;
}

class GoodPlaceTheme {
  static final ThemeData lightTheme = ThemeData(
    fontFamily: 'SulSans',
  );
}
